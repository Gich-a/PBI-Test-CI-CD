name: Deploy Power BI
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: windows-latest
    steps:
      - name: 📦 Install Power BI PowerShell Modules
        shell: pwsh
        run: Install-Module -Name MicrosoftPowerBIMgmt -Force -AcceptLicense

      - name: 🚀 Deploy PBIP to DEV Workspace
        shell: pwsh
        run: |
          if (!(Test-Path "scripts/deploy-pbip.ps1")) {
            throw "❌ Script file not found at 'scripts/deploy-pbip.ps1'"
          }
          & "scripts/deploy-pbip.ps1"
